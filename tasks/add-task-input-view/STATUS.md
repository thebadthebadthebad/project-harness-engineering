# STATUS

## Status


doing

허용값은 `todo`, `doing`, `completed`, `stopped`다.

## Final Goal


Task show에서 bounded input의 경로, 크기와 digest를 사람이 검토할 수 있게 표시한다.

## Work Plan


| Work | Status |
| --- | --- |
| Task View 렌더링 보정 | doing |
| bounded-input 회귀 테스트 | todo |
| 전체 회귀와 공식 반영 | todo |

Work Status는 `todo`, `doing`, `completed` 중 하나를 사용한다.

## Current Work

Task View 렌더링 보정

Task가 `completed` 또는 `stopped`이면 `None`으로 작성한다.
