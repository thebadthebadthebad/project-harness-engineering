# STATUS

## Status


stopped

허용값은 `todo`, `doing`, `completed`, `stopped`다.

## Final Goal


Phase 0–2 Minimal Vertical Slice로 v2 canonical records, legacy dry-run adapter, local runtime, 격리 worktree, typed handoff와 검증된 Promotion 흐름을 구현하고 fault test로 검증한다.

## Work Plan


| Work | Status |
| --- | --- |
| Phase 0 baseline과 v1 fixture 기준 고정 | completed |
| v2 canonical schema와 validator | todo |
| legacy inspect/plan/parity adapter | todo |
| local runtime과 isolated command runner | todo |
| typed handoff와 staged Promotion | todo |
| fault injection과 전체 회귀 검증 | todo |
| REPORT와 구현 후 handoff 정리 | todo |

Work Status는 `todo`, `doing`, `completed` 중 하나를 사용한다.

## Current Work

None

Task가 `completed` 또는 `stopped`이면 `None`으로 작성한다.
