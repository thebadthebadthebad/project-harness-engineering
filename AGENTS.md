# AGENTS

## Rules

- Task 생성, 활성화, 상태 확인, 완료 확인은 `python project/tools/projectctl.py --root . task ...` 명령을 사용한다.
- `project/`는 공용 배포 템플릿이다. Task 결과를 반영하기 전 계획을, 반영한 뒤 diff와 검증 결과를 사용자에게 확인받는다.
- 이 저장소에만 필요한 공통 코딩·문서 규칙이 생기면 이 문서에 추가한다.
